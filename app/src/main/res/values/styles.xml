<?xml version="1.0" encoding="utf-8"?>
<resources>
    <style name="TextJudulHalaman">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">@font/nunito_bold</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">24sp</item>
        <item name="android:gravity">center</item>
        <item name="android:layout_marginTop">10dp</item>
    </style>

    <style name="TextSubJudul">
        <item name="android:layout_width">wrap_content</item>
        <item name="android:layout_height">wrap_content</item>
        <item name="android:fontFamily">@font/nunito_bold</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:textSize">24sp</item>
        <item name="android:layout_marginTop">20dp</item>
        <item name="android:layout_marginStart">20dp</item>
    </style>

    <style name="ButtonProfil">
        <item name="android:layout_width">match_parent</item>
        <item name="android:layout_height">60dp</item>
        <item name="android:backgroundTint">@android:color/transparent</item>
        <item name="android:textColor">@color/black</item>
        <item name="android:fontFamily">@font/nunito_bold</item>
        <item name="cornerRadius">0dp</item>
        <item name="strokeColor">@color/abu2</item>
        <item name="strokeWidth">2dp</item>
        <item name="iconGravity">start</item>
        <item name="iconSize">20dp</item>
    </style>

    <style name="BottomNavigationViewStyle" parent="Widget.MaterialComponents.BottomNavigationView">
        <item name="itemRippleColor">@android:color/transparent</item>
    </style>

    <!-- Overlay untuk Date/Range Date Picker (MDC) -->
    <!-- Overlay untuk Date/Range Date Picker -->
    <style name="ThemeOverlay.App.DatePicker" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <!-- warna permukaan dialog -->
        <item name="colorSurface">@color/light_gray</item>
        <!-- warna teks utama di dialog -->
        <item name="colorOnSurface">@android:color/black</item>

        <!-- style kalender di dalam dialog -->
        <item name="materialCalendarStyle">@style/Widget.App.MaterialCalendar</item>
    </style>

    <style name="Widget.App.DatePicker.TextButton" parent="Widget.Material3.Button.TextButton">
        <item name="android:textColor">@android:color/black</item>
    </style>

    <!-- Kalender -->
    <style name="Widget.App.MaterialCalendar" parent="Widget.MaterialComponents.MaterialCalendar">
        <item name="android:background">@color/light_gray</item>

        <!-- Judul header (bulan/tahun) -->
        <item name="materialCalendarHeaderTitle">@style/TextAppearance.App.DatePicker.Header</item>

    </style>

    <style name="TextAppearance.App.DatePicker.Header" parent="TextAppearance.Material3.TitleMedium">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="TextAppearance.App.DatePicker.DayOfWeek" parent="TextAppearance.Material3.LabelMedium">
        <item name="android:textColor">@android:color/white</item>
    </style>

    <style name="Widget.App.MaterialCalendar.Day" parent="Widget.MaterialComponents.MaterialCalendar.Day">
        <item name="android:textColor">@android:color/white</item>
    </style>


</resources>